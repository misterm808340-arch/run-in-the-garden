{
  "name": "run-in-the-garden",
  "version": "1.0.0",
  "private": true,
  "description": "Run in the Garden - offline-first garden endless runner (PWA + Capacitor + AdMob test mode)",
  "scripts": {
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "apk:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "apk:release": "cap sync android && cd android && ./gradlew assembleRelease",
    "aab:release": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor-community/admob": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
